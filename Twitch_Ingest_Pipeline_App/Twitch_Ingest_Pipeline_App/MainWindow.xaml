<Window x:Class="Twitch_Ingest_Pipeline_App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Twitch Ingest Pipeline Console"
        Height="700"
        Width="1000"
        MinHeight="600"
        MinWidth="900">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="180"/>
            <RowDefinition Height="220"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Twitch Ingest Pipeline Console"
                   FontSize="24"
                   FontWeight="Bold"
                   Margin="0,0,0,16"/>

        <!-- Top row -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="24"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Service Status -->
            <GroupBox Grid.Column="0" Header="Service Status" Padding="12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="140"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="IRC Health:" Margin="0,0,8,10" FontWeight="SemiBold"/>
                    <TextBlock x:Name="IRCHealthTextBlock" Grid.Row="0" Grid.Column="1" Text="Unknown" Margin="0,0,0,10"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="IRC Ready:" Margin="0,0,8,10" FontWeight="SemiBold"/>
                    <TextBlock x:Name="IRCReadyTextBlock" Grid.Row="1" Grid.Column="1" Text="Unknown" Margin="0,0,0,10"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Last Refresh:" Margin="0,0,8,10" FontWeight="SemiBold"/>
                    <TextBlock x:Name="LastRefreshTextBlock" Grid.Row="2" Grid.Column="1" Text="Never" Margin="0,0,0,10"/>

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Account:" Margin="0,0,8,0" FontWeight="SemiBold"/>
                    <TextBlock x:Name="AccountTextBlock" Grid.Row="3" Grid.Column="1" Text="Unknown"/>
                </Grid>
            </GroupBox>

            <!-- Channel Control -->
            <GroupBox Grid.Column="2" Header="Channel Control" Padding="12">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="12"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="12"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Channel:" VerticalAlignment="Center" Margin="0,0,12,0" FontWeight="SemiBold"/>
                    <TextBox x:Name="ChannelInputTextBox"
                 Grid.Row="0"
                 Grid.Column="1"
                 Height="28"
                 VerticalContentAlignment="Center"
                 ToolTip="Enter a Twitch channel name, e.g. lirik"/>

                    <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal">
                        <Button x:Name="JoinButton"
                    Content="Join"
                    Width="100"
                    Height="32"
                    Margin="0,0,12,0"/>
                        <Button x:Name="PartButton"
                    Content="Part"
                    Width="100"
                    Height="32"/>
                    </StackPanel>

                    <Button x:Name="RefreshButton"
                Grid.Row="4"
                Grid.Column="1"
                Content="Refresh"
                Width="100"
                Height="32"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"/>
                </Grid>
            </GroupBox>
        </Grid>

        <!-- Desired Channels -->
        <GroupBox Grid.Row="2" Header="Desired Channels" Padding="12" Margin="0,0,0,16">
            <Grid>
                <ListBox x:Name="ChannelsListBox"/>
            </Grid>
        </GroupBox>

        <!-- Activity Log -->
        <GroupBox Grid.Row="3" Header="Activity / Response Log" Padding="12">
            <Grid>
                <TextBox x:Name="ActivityLogTextBox"
                         IsReadOnly="True"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"
                         TextWrapping="Wrap"/>
            </Grid>
        </GroupBox>
    </Grid>
</Window>